const { List } = require('./list.model').default;

const { Task } = require('./task.model').default;
const { User } = require('./user.model').default;


module.exports = {
    List,
    Task,
    User
}